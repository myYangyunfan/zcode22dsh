{
  "id": "dsh-zcode-migrate",
  "version": "0.1.0",
  "main": "./src/index.js",
  "description": "zcode → dsh 会话迁移：把 zcode CLI 的历史会话转换成 dsh 原生会话日志（session.jsonl.zstd），迁移后可在 dsh 里直接恢复",
  "engines": {
    "dsh": ">=0.0.1"
  },
  "contributes": {
    "tools": [
      "zcode.inspect",
      "zcode.migrate",
      "zcode.verify"
    ],
    "skills": []
  },
  "client": {
    "main": "./lib/client.js"
  }
}
